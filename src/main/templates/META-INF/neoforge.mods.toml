license="${mod_license}"
[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
logoFile="icon.png"
authors="MarbleGate"
description='''
SuperPipeSlide adds anchor-based pipe routes that let players slide along pipe surfaces without vehicles.
'''

${sodium_config_api_block}
[[mixins]]
config="${mod_id}.mixins.json"

${integration_mixin_blocks}

[[dependencies.${mod_id}]]
    modId="neoforge"
    type="required"
    versionRange="[${neo_version},)"
    ordering="NONE"
    side="BOTH"

[[dependencies.${mod_id}]]
    modId="minecraft"
    type="required"
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="BOTH"
